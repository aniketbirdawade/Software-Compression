cd ~/SoftwareCompression/SPC/Demo
ls
app.py
venv
source venv/bin/activate
python3 app.py
Running on http://127.0.0.1:5000
---------------------------------------------------
psql postgres
create database slideShow;
\c slideshow
create table users(id serial primary key, username varchar(100) unique not null, password varchar(100) not null);
